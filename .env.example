# A generated secret for Payload
PAYLOAD_SECRET=

# Kaizen inquiry intake endpoint (includes the intake token) and its HMAC
# signing secret — get both from the Kaizen team; never commit real values
KAIZEN_INQUIRY_URL=
KAIZEN_INQUIRY_SECRET=
